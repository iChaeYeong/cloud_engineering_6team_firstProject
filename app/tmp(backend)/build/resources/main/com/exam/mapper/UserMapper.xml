<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.mapper.UserMapper">

    <select id="findById" resultType="UserDTO" parameterType="String">
        select user_id  as userId,
               user_nm  as userNm,
               pwd
        from user_info
        where user_id = #{userId}
    </select>

    <insert id="save" parameterType="UserDTO">
        insert into user_info (user_id, user_nm, pwd)
        values (#{userId}, #{userNm}, #{pwd})
    </insert>

</mapper>
